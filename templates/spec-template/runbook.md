# Runbook

Document the exact workflow used to create, evaluate, publish, download, and serve the model.

## Commands

```bash
tt eval -f tunedtensor.json
tt push -f tunedtensor.json
```

## Results

Add run IDs, dataset IDs, model IDs, and links when they are public.
